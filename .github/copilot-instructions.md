# Agent Instructions

## Visual Changes

All visual or UI changes must be confirmed using the available browser tools (e.g., `open_browser_page`, `screenshot_page`, `read_page`). Do not consider a visual change complete until you have opened the app in the browser and verified the result matches expectations. If the browser tools are unavailable, ask the user to confirm visually before marking the task done.
